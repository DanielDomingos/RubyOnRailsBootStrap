<p style="color: green"><%= notice %></p>
<div class="col-lg-8 mx-auto">
  <h1>Plants</h1>
  <%= link_to "Nova planta", new_plant_path, class:"btn btn-primary btn-lg mb-3" %>
  <div id="plants">
    <% @plants.each do |plant| %>
      <div class="card shadow mb-3">
        <div class="card-body">
          <%= render plant %>
          <p>
            <%= link_to "Detalhes", plant, class:"btn btn-outline-primary btn-lg" %>
          </p>
        </div>
      </diV>
      <% end %>
  </div>
</div>